# 气象数据自动化采集脚本需求文档

## 1. 项目背景

本项目是一个气象分析系统，旨在通过自动化手段采集和存储气象预报数据，为后续的分析和应用提供基础数据支持。目前项目已有的 `api_forecast_hourly()` 方法可以获取未来 10 天的逐小时预报数据，需要开发一个自动化脚本将这些数据存入 Doris 数据库。

## 2. 功能需求

### 2.1 初始化功能
- 导入项目核心类 `HtMeteo`
- 调用 `set_forecast_mode('on')` 启用预报模式
- 加载四川省内所有区县的地点信息

### 2.2 数据获取功能
- 调用 `api_forecast_hourly()` 接口获取逐小时预报数据
- 支持批量处理多个地点的数据
- 处理数据获取失败的情况

### 2.3 数据库操作功能
- 连接 Doris 数据库
- 创建 `hourly_forecast` 表
- 实现数据的插入和更新逻辑，确保同一地点同一时间的数据唯一

### 2.4 异常处理功能
- 处理数据库连接异常
- 处理数据获取失败的情况
- 处理认证失败的情况

## 3. 技术方案

### 3.1 技术栈
- Python 3.13+
- pandas 2.3.3+
- pymysql (用于连接 Doris 数据库)
- tqdm (用于进度显示)

### 3.2 核心流程
1. 初始化 `HtMeteo` 实例并设置预报模式
2. 加载四川省内所有区县的地点信息
3. 创建 `hourly_forecast` 表
4. 遍历每个地点，获取逐小时预报数据
5. 将数据插入或更新到数据库中
6. 处理异常情况并记录日志

### 3.3 数据流程
- 从 API 获取 JSON 格式的数据
- 转换为 pandas DataFrame
- 处理数据格式和缺失值
- 插入或更新到 Doris 数据库

## 4. 数据库设计

### 4.1 表结构

| 字段名 | 数据类型 | 描述 |
| :--- | :--- | :--- |
| location | VARCHAR(100) | 地点名称 |
| datetime | DATETIME | 预报时间 |
| temperature_2m | FLOAT | 2米高度气温 |
| precipitation | FLOAT | 降水量 |
| shortwave_radiation_instant | FLOAT | 短波辐射 |
| wind_speed_10m | FLOAT | 10米高度风速 |
| relative_humidity_2m | FLOAT | 2米高度相对湿度 |

### 4.2 主键设计
- 使用 `location` 和 `datetime` 作为复合主键，确保同一地点同一时间的数据唯一

### 4.3 分区设计
- 按 `datetime` 字段进行范围分区，分为 2024、2025、2026 等年份的分区

### 4.4 分桶设计
- 按 `location` 字段进行哈希分桶，提高查询性能

## 5. 实现计划

### 5.1 开发步骤
1. 创建脚本文件 `scripts/weather_to_sqlite.py`
2. 实现初始化和地点加载功能
3. 实现数据获取功能
4. 实现数据库连接和表创建功能
5. 实现数据插入和更新功能
6. 测试脚本运行情况
7. 优化异常处理和日志记录

### 5.2 测试计划
1. 测试地点加载功能
2. 测试数据获取功能
3. 测试数据库连接和表创建功能
4. 测试数据插入和更新功能
5. 测试异常处理功能

## 6. 验收标准

### 6.1 功能验收
- 脚本能够成功加载四川省内所有区县的地点信息
- 脚本能够成功获取逐小时预报数据
- 脚本能够成功创建 `hourly_forecast` 表
- 脚本能够成功将数据插入或更新到数据库中
- 脚本能够处理异常情况并给出合理的错误提示

### 6.2 性能验收
- 脚本能够在合理的时间内处理所有地点的数据
- 数据库插入和更新操作能够高效执行
- 脚本能够处理大量数据而不出现内存溢出

### 6.3 数据质量验收
- 数据库中的数据与 API 返回的数据一致
- 数据格式正确，无缺失值
- 同一地点同一时间的数据唯一

## 7. 风险评估

### 7.1 潜在风险
- API 认证失败，导致无法获取数据
- 数据库连接失败，导致数据无法入库
- 数据量过大，导致脚本运行时间过长
- 网络不稳定，导致数据获取中断

### 7.2 应对措施
- 增加认证失败的处理逻辑，给出明确的错误提示
- 增加数据库连接异常的处理逻辑，确保脚本能够正常运行
- 优化数据处理逻辑，提高脚本运行效率
- 增加网络异常的处理逻辑，支持断点续传

## 8. 后续维护

### 8.1 维护计划
- 定期检查脚本运行状态
- 定期更新地点信息
- 定期检查数据库表结构和数据质量
- 定期优化脚本性能

### 8.2 扩展计划
- 支持更多地点的气象数据采集
- 支持更多类型的气象数据采集
- 支持数据可视化和分析功能
- 支持数据导出和共享功能
